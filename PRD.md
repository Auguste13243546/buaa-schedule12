# 个人日程管理系统 · 第一版 PRD

| 项 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 编写日期 | 2026-09-25 |
| 适用范围 | 仅第一版（MVP） |
| 使用者 | 单一用户（北京航空航天大学本科生，2026—2027 秋季学期） |
| 文档地位 | 后续开发的唯一依据。与实现冲突时，以本文件为准并先修订本文件 |

需求来源：用户口述 + 北航 2026—2027 学年校历图 + 教务系统课表截图（2026—2027 秋季 第 3 周视图）。所有推断项在第 5.5 节集中列出。

---

## 1. 产品目标和使用场景

### 1.1 产品目标

一句话：**在任意设备上，用一个浏览器链接，三秒钟看清「今天要干什么」，并能把要做的事记下来。**

拆成三个可验证的目标：

| 目标 | 含义 | 达成判据 |
| --- | --- | --- |
| G1 一眼看清今天 | 打开默认落在今日视图，不需要任何点击 | 首屏可见今日课程 + 今日待办 |
| G2 记得住、下次还在 | 关闭浏览器再打开，自己录入的日程不丢失 | 刷新/重开后日程列表不变 |
| G3 两端都能读 | 手机竖屏和电脑宽屏都不出现横向滚动、文字挤压 | 390px 与 1280px 下三个视图均可正常操作 |

非目标：不做多设备同步、不做协作、不做提醒推送。见第 8 章。

### 1.2 使用场景

**场景 A · 早上出门前（手机）**
07:40，手机上点开链接。看到「9月25日 星期五 · 第 3 周 · 中秋节」，今日课程一条：08:00–11:25 习近平新时代中国特色社会主义思想，科研一号楼。想起昨晚作业没交，划掉它，关页面。全程不切换视图。

**场景 B · 课间确认下一节去哪（手机）**
11:30 下课，打开课表视图，默认停在当前周，切到「周三」看到 09:50 综合法语(1) 在教学二号楼。

**场景 C · 晚上在宿舍排下周（电脑）**
宽屏下用 7×14 网格通览整周，发现下周二第 3–5 节的计算机基础机房课和别的事冲突，点开那一节选「停掉这一节」，再在空格里补一节。

**场景 D · 临时通知调课**
班级群通知「本周五下午航空航天概论A 改为线上」。手机上课表视图（手机端为单日列表），选周五，点开 14:50 那一节，停课。

**场景 E · 学期中途换课表**
教务系统里换了教室或课程增减。这类改动**不在页面内编辑**，需要修改 `js/courses.js` 并重新部署。这是刻意的设计边界，见 5.4。

---

## 2. 整体页面框架和导航结构

### 2.1 技术形态

纯静态单页应用，无框架、无构建步骤、无后端。用普通 `<script>` 标签而非 ES module，保证双击 `index.html`（`file://` 协议）也能直接运行——这是为了本地预览零门槛。

```
E:\schedule\
├─ index.html      页面骨架、三个视图容器、抽屉容器
├─ style.css       全部样式与响应式断点
└─ js\
   ├─ calendar.js  校历常量、节次表、周次与日期计算
   ├─ courses.js  固定课表数据
   ├─ store.js    localStorage 读写 + 当天实际课表解析
   └─ app.js      路由、渲染、事件
```

### 2.2 导航结构

三个平级视图，无层级、无子页。Hash 路由，可收藏、可前进后退：

| 路由 | 视图 | 默认 |
| --- | --- | --- |
| `#/today` | 今日 | 是（无 hash 或 hash 非法时回落到此） |
| `#/schedule` | 课表 | 否 |
| `#/todos` | 日程 | 否 |

三视图之间是**并列**关系，不存在从属跳转；唯一的跨视图动作是「今日」页的「+ 添加待办」直接打开「日程」的新增表单。

### 2.3 全局骨架 · 电脑端（≥768px）

```
┌──────────────────────────────────────────────────────────────┐
│  今日                                    [ 今日 | 课表 | 日程 ]│ ← 顶栏，sticky
│  2026—2027 秋季学期 · 第 3 周                                  │   标题 + 副标题 + 右侧标签
├──────────────────────────────────────────────────────────────┤
│                                                              │
│                     内容区（居中，最大宽 1080px）              │
│                                                              │
│   ┌────────────────────────────────────────────────────┐     │
│   │  卡片                                              │     │
│   └────────────────────────────────────────────────────┘     │
│   ┌────────────────────────────────────────────────────┐     │
│   │  卡片                                              │     │
│   └────────────────────────────────────────────────────┘     │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 2.4 全局骨架 · 手机端（<768px）

顶栏的标签导航隐藏，改为底部固定标签栏（拇指可及）；内容区底部预留 84px 避让。

```
┌────────────────────────────┐
│ 今日                        │ ← 顶栏，sticky，只留标题+副标题
│ 2026—2027 秋季学期 · 第3周  │
├────────────────────────────┤
│                            │
│  ┌──────────────────────┐  │
│  │ 卡片                  │  │
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │ 卡片                  │  │
│  └──────────────────────┘  │
│                            │
├────────────────────────────┤
│   [ 今日 ]  [ 课表 ]  [ 日程 ]│ ← 底部标签栏，三等分，fixed
└────────────────────────────┘
```

### 2.5 断点策略

只有一个断点：768px。

| 位置 | <768px | ≥768px |
| --- | --- | --- |
| 导航 | 底部标签栏 | 顶栏右侧标签 |
| 课表 | 星期选择条 + 单日纵向列表 | 7 列 × 14 行网格 |
| 表单抽屉 | 底部上滑，占屏高 ≤86% | 屏幕居中弹窗，宽 ≤440px |

课表的两套结构在 DOM 里同时存在，靠 CSS 显隐。这样横竖屏旋转、窗口拖宽拖窄都不需要重新渲染，代价是多一份 DOM（约 130 个节点，可接受）。

### 2.6 交互总则

1. **点卡片正文 = 打开编辑/详情**，点圆形勾选框 = 直接切换完成状态。两者不冲突，勾选不需要先进入详情。
2. **所有删除/停课都必须可撤销**。停课走「本周调课」列表的撤销按钮；删除日程在抽屉内二次确认式按钮（点删除即生效，但抽屉内取消按钮在旁边）。
3. **不使用长按、不使用滑动删除、不使用拖拽**。手机端微信内置浏览器对手势支持不一，且这些交互不可发现。
4. **表单提交后一律关闭抽屉并整页重渲染**，不做局部 diff。数据量小，重渲染成本可忽略，换来的是状态永远一致。
5. **校验只在提交时做**，失败用 `alert` 说明原因，不做实时红字。
6. **文字一律不换行截断用于网格内**（课表格子用省略号），列表内允许换行。

---

## 3. 首页（今日）的内容结构

首页是产品目标 G1 的唯一载体，因此信息优先级被严格排序：**日期身份 → 课程 → 待办**。

### 3.1 内容层级

| 顺序 | 区块 | 内容 | 为什么在这个位置 |
| --- | --- | --- | --- |
| 1 | 日期头 | `9月25日` 大字 + `星期五 · 第 3 周` + 节假日标签 | 用户最先要确认的是「今天算哪天」，周次决定课表是否正确 |
| 2 | 今日课程 | 按开始节次升序的时间线 | 课程是硬约束，时间不可挪，优先级高于待办 |
| 3 | 今日待办 | 未完成在前、已完成置灰在后 | 可自主安排的事 |

### 3.2 文字概念图 · 手机端

```
┌────────────────────────────┐
│ 今日                        │
│ 2026—2027 秋季学期 · 第3周  │
├────────────────────────────┤
│                            │
│ 9月25日                     │ ← 26px 粗体
│ 星期五 · 第 3 周 [中秋节]    │ ← 13.5px 灰字 + 红色圆角标签
│                            │
│ ╭────────────────────────╮ │
│ │ 今日课程                │ │
│ │                        │ │
│ │ 08:00–11:25  ▌习近平新时代 │ │ ← 左侧 3px 色条，色条颜色 = 课程色
│ │ 第1–4节      ▌科研一号楼   │ │   时间列固定宽 96px，右对齐
│ │                        │ │
│ │ 14:50–16:35  ▌航空航天概A │ │
│ │ 第7–8节      ▌教学一号楼   │ │
│ │                        │ │
│ │ 19:00–20:35  ▌综合法语(1) │ │ ← 若当前时间已过下课点，整行 opacity .45
│ │ 第11–12节    ▌教室待定    │ │
│ ╰────────────────────────╯ │
│                            │
│ ╭────────────────────────╮ │
│ │ 今日待办   未完成 2      │ │
│ │                        │ │
│ │ ○  法语课后作业         │ │ ← 点击圆环 = 划掉
│ │    ●作业 · 全天         │ │   点击文字区 = 打开编辑
│ │                        │ │
│ │ ◉  交团费               │ │ ← 已完成：圆环实心、标题加删除线
│ │    ●生活 · 14:00–14:30  │ │
│ │                        │ │
│ │ ┌────────────────────┐ │ │
│ │ │   + 添加待办        │ │ │ ← 次要按钮，通栏描边
│ │ └────────────────────┘ │ │
│ ╰────────────────────────╯ │
│                            │
│  [ 今日 ] [ 课表 ] [ 日程 ] │
└────────────────────────────┘
```

### 3.3 电脑端差异

结构完全相同，不做多栏。理由：今日信息量小，两栏会让时间线和待办的对齐关系变复杂，收益不成立。仅内容区居中收窄到 1080px，卡片内边距增大。

### 3.4 空态与边界态

| 情况 | 显示 |
| --- | --- |
| 今天没有课（周次内） | 「今天没有课。」 |
| 今天不在学期周内（寒暑假/开学前） | 「不在学期周内，没有课程。」，日期头不显示周次 |
| 学期已结束 | 顶栏副标题改为「2026—2027 秋季学期 已结束」，今日课程走上面的空态文案 |
| 今天没有待办 | 「今天没有安排。」 |
| 待办全部完成 | 计数显示「全部完成」 |

空态必须是一句人话，不留空白区域——这是 G1「一眼看清今天」的一部分：看清包括看清「没有」。

---

## 4. 每个模块的用途

| 模块 | 解决的问题 | 用户什么时候用它 | 用完了得到什么 |
| --- | --- | --- | --- |
| **今日** | 「我现在该做什么 / 今天有什么」 | 每天多次，最高频 | 认知确认，无需产出 |
| **课表** | 「周几第几节在哪个楼」「这周整周什么安排」 | 看下一节、规划一周、执行调课 | 只读认知 + 调课记录 |
| **日程** | 「怕忘的事记下来」 | 随时记录，每天勾选 | 一条持久化的待办 + 完成状态 |
| **校历层**（无界面） | 把「第几周」这个教务概念换算成具体日期和节次时刻 | 用户不直接操作 | 供上面三个模块查询 |
| **存储层**（无界面） | 让用户的录入活过刷新 | 用户不直接操作 | 持久化 |

校历层和存储层没有 UI，但它们是模块，PRD 里必须定义，因为第 5 章的数据归属依赖这个划分。

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 校历层（`calendar.js`，只读常量）

```js
TERM = { label, startMonday:'2026-09-07', totalWeeks:19, courseEndWeek:16 }
SESSIONS = [ {no, start, end, part} × 14 ]
HOLIDAYS = { 'YYYY-MM-DD': '名称' }
```

节次时刻表（完整录入，来自校历图右侧「教学作息时间表」）：

| 段 | 节次 | 时刻 |
| --- | --- | --- |
| 上午 | 1 | 08:00–08:45 |
| 上午 | 2 | 08:50–09:35 |
| 上午 | 3 | 09:50–10:35 |
| 上午 | 4 | 10:40–11:25 |
| 上午 | 5 | 11:30–12:15 |
| 下午 | 6 | 14:00–14:45 |
| 下午 | 7 | 14:50–15:35 |
| 下午 | 8 | 15:50–16:35 |
| 下午 | 9 | 16:40–17:25 |
| 下午 | 10 | 17:30–18:15 |
| 晚上 | 11 | 19:00–19:45 |
| 晚上 | 12 | 19:50–20:35 |
| 晚上 | 13 | 20:40–21:25 |
| 晚上 | 14 | 21:30–22:15 |

操作：无。用户不可在页面内修改校历。

### 5.2 课表层（`courses.js`，只读常量）

字段：`id`、`name`、`teacher`、`color`、`weeks{start,end,parity}`、`slots[{day,start,end,room}]`。

一门课多个时段存成一条记录 + 多个 slot，因此「综合法语(1)」的 4 个时段是一个整体。

录入数据（9 门课 / 12 个时段）：

| 课程 | 时段 | 教室 | 主色 |
| --- | --- | --- | --- |
| 综合法语(1) | 周一6–7、周二6–7、周三3–4、周四11–12 | 教学二号楼（周四待定） | `#7c3aed` |
| 体育(1) | 周一8–9 | 杭州田径场 | `#dc2626` |
| 综合法语实训(1) | 周二1–2 | 教学二号楼 | `#9333ea` |
| 大学计算机基础 | 周二3–5 | 计算机房 | `#ea580c` |
| 基础英语(1) | 周二8–9 | 教学一号楼 | `#e11d48` |
| 心理健康(1) | 周三11–12 | 待定 | `#f97316` |
| 数学基础 | 周四6–7 | 科研一号楼 | `#8b5cf6` |
| 习近平新时代中国特色社会主义思想 | 周五1–4 | 科研一号楼 | `#ef4444` |
| 航空航天概论A | 周五6–7 | 教学一号楼 | `#a855f7` |

全部课程：第 1–16 周，`parity:'all'`（无单双周）。`teacher` 字段留空。

操作：只读。修改需重新部署。

### 5.3 日程模块（用户数据，可写）

字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 自动 | 随机串 |
| `title` | 是 | ≤60 字 |
| `date` | 是 | `YYYY-MM-DD`，日期选择器 |
| `startTime` / `endTime` | 否 | 留空 = 全天 |
| `category` | 是 | 作业 / 考试 / 生活（默认）/ 社团 |
| `note` | 否 | 多行 |
| `done` | 自动 | 勾选切换 |

最必要的操作，四个，缺一不可：**新增、编辑、删除、勾选完成**。
筛选：按分类。排序：按日期 + 开始时间。分组：已逾期 / 今天 / 以后 / 已完成。

明确不做的字段：地点、提醒时间、重复规则、优先级、附件。

### 5.4 调课模块（用户数据，可写）

| 动作 | 字段 | 语义 |
| --- | --- | --- |
| 停课 `cancel` | `date`、`slotKey`、`start`、`end`、`label` | 只停 `date` 这一天这一节，同课程其他周不受影响 |
| 补课 `extra` | `date`、`day`、`name`、`start`、`end`、`room` | 在指定日期插入一节临时课 |

`slotKey = courseId|day|start`，是停课的精确寻址键。

操作：**停课、补一节课、撤销任意一条**。撤销必须存在，否则误操作无法恢复。

### 5.5 推断项清单（未经用户逐条核对，如错按此表改）

| # | 推断 | 依据 |
| --- | --- | --- |
| P1 | 所有课程 1–16 周 | 用户回复「默认都对」；校历第 17 周起为考试周 |
| P2 | 无单双周课程 | 第 3 周截图无法体现单双周差异 |
| P3 | 周一第 5 节无课 | 截图该格色块边界不清，用户未纠正 |
| P4 | 周五「习近平新时代…」为第 1–4 节 | 同上 |
| P5 | 周三心理健康、周四综合法语为第 11–12 节 | 截图底部被裁切 |
| P6 | 第 13、14 节无课 | 截图未滚到 |
| P7 | 周四/周三晚间课教室待定 | 截图未显示 |

### 5.6 存储契约

```
key   : buaa-schedule:v1
value : { version:1, todos:[...], overrides:[...] }
```

`version` 不等于 1 或 JSON 解析失败时，回落为空数据并 `console.warn`，不抛错白屏。写入失败（隐私模式、配额满）弹 `alert` 提示。

数据归属划分是本版本最关键的一条设计约束：

| 数据 | 归属 | 生效范围 |
| --- | --- | --- |
| 校历、课表 | 编译期常量 | 所有设备，改代码 + 重新部署才变 |
| 日程、调课 | 运行时 localStorage | **仅当前浏览器**，换设备看不到 |

---

## 6. 首页、今日计划与各模块之间的关系

### 6.1 数据流

```
                    ┌──────────────┐
                    │ calendar.js  │ 校历常量 + 周次/日期换算
                    └──────┬───────┘
                           │ weekOfDate() / SESSIONS / dateOf()
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
 ┌────────────┐     ┌────────────┐    ┌────────────┐
 │ courses.js │     │ store.js   │    │  当前时间   │
 │ 固定课表    │────▶│ localStorage│   │ todayStr() │
 └─────┬──────┘     │ todos      │    │ nowMinutes │
       │            │ overrides  │    └─────┬──────┘
       │            └───────────┘          │
       └──────────┬───────┘                 │
                  ▼                         │
          lessonsOn(date)  ◀────────────────┘
          「某天实际上课表」= 固定课表按周次过滤
            → 减去当天 cancel → 加上当天 extra → 按节次排序
                  │
      ┌───────────┴───────────┐
      ▼                       ▼
  renderToday()          renderSchedule()
  今日课程时间线          网格 / 单日列表
      │                       │
      └───────────┬───────────┘
                  ▼
              renderTodos()
              日程分组列表
```

### 6.2 关键关系定义

**R1 · `lessonsOn(date)` 是唯一的事实来源。** 今日视图和课表视图都调用它，因此「今日显示有课」和「课表那一格有课」逻辑上不可能不一致。停课在今日页生效，在课表页也同时生效。

**R2 · 今日视图是课表视图在「当前日期」上的切片，不是独立数据。** 不存在「今日课程」这张表，它每次渲染现算。

**R3 · 今日待办是日程模块在「今天」上的切片。** 同一份 `todos`，同一套勾选逻辑，在今日页勾选完成后日程页里该项自动进入「已完成」组。

**R4 · 校历层是换算器，不是数据源。** 它不提供 UI，只回答「今天第几周」「第 6 节几点上课」。周次算法：`week = floor((今天 − 2026-09-07) / 7) + 1`，超出 19 周返回 null。

**R5 · 节假日只标注，不干预排课。** `HOLIDAYS` 命中时在日期头挂一个红标签，但**不会**自动隐藏当天课程。国庆、中秋当天是否上课，由用户手动停课决定。这是刻意的：校历图上节假日的具体放假区间我无法从截图完全确认，自动停课一旦判错，用户会漏课，比多显示一节严重得多。

**R6 · 视图切换不改变数据，只改变投影。** 三个视图共享同一份 `state`，任一视图内的写操作触发 `onChange(render)`，全量重渲染当前视图。

### 6.3 各页面布局与交互细节

**课表 · 电脑端网格**

```
┌──────────────────────────────────────────────────────────────┐
│  ‹   第 3 周   ›        9月21日 – 9月27日     [回到本周]        │
├──┬─────┬─────┬─────┬─────┬─────┬─────┬─────┬──────────────────┤
│节│ 一  │ 二  │ 三  │ 四  │ 五  │ 六  │ 日  │  ← 表头显示周几+日期│
│次│9.21 │9.22 │9.23 │9.24 │9.25 │9.26 │9.27 │     今天高亮为蓝色  │
├──┼─────┼─────┼─────┼─────┼──────────┼─────                  │
│1 │     │▞法语│     │     │▞习思│     │     │  行高 46px        │
│2 │     │▞实训│     │     │▞ 想 │     │     │  共 14 行         │
│3 │     │▞计算│▞法语│     │▞  │  │     │     │                  │
│4 │     │▞ 机 │▞ (1)│▞  │  │     │     │     │  色块跨行 = 连堂  │
│5 │     │▞ 基 │     │     │   │  │     │     │                  │
│6 │▞法语│▞法语│     │▞数学│航空│     │     │  点色块 → 详情抽屉  │
│7 │▞(1) │▞(1) │     │▞基础│▞概论│     │     │  点空格 → 补课抽屉  │
│8 │▞体育│▞基础│     │     │     │     │     │                  │
│9 │▞(1) │▞英语│     │     │     │     │     │                  │
│… │     │▞(1) │     │     │     │     │     │                  │
│11│     │     │▞心理│▞法语│     │     │     │                  │
│12│     │     │▞健康│▞(1) │     │     │     │                  │
├──┴─────┴─────┴─────┴─────┴─────┴─────┴─────┤                  │
│  本周调课                                    │                  │
│  2026-09-25 · 停课 · 航空航天概论A 第7–8节 [撤销]│                │
└──────────────────────────────────────────────┘
```

交互：`‹ ›` 翻周，范围钳制在 1–19 周；非当前周时出现「回到本周」；翻到 17 周以上时网格为空并提示「已超过第 16 周，按校历不再排课」。网格下方「本周调课」卡片列出本周所有停/补记录并提供撤销——**这是唯一的撤销入口**，必须存在。

**课表 · 手机端单日视图**

```
┌────────────────────────────┐
│  ‹  第 3 周  ›  9.21–9.27   │
├────────────────────────────┤
│ ──────────────────────── │
│ │ 9月25日 星期五           │ │
│ │ [一][二][三][四][五][六][日]│ │ ← 横向可滚的星期选择条
│ │  2    4   2   2   2   0  0│ │   每个 chip 带当天课数角标
│ │                        │ │
│ │ 08:00–11:25 ▌习近平新时代 │ │ ← 复用今日页的 lesson 行样式
│ │ 第1–4节      ▌科研一号楼   │ │
│ │                        │ │
│ │ 14:50–16:35 ▌航空航天概论A│ │
│ │ 第7–8节      ▌教学一号楼   │ │
│ │                        │ │
│ │ ┌────────────────────┐ │ │
│ │ │   + 补一节课        │ │ │
│ │ └────────────────────┘ │ │
│ ╰────────────────────────╯ │
│  [ 今日 ] [ 课表 ] [ 日程 ] │
└────────────────────────────┘
```

**日程页**

```
┌────────────────────────────┐
│ 日程                        │
├────────────────────────────┤
│ [全部][作业][考试][生活][社团]│ ← 分类筛选，单选
│                            │
│ ╭────────────────────────╮ │
│ │ 已逾期  1               │ │
│ │ ○ 实验报告    ●作业 9.20 │ │ ← 逾期未完成单独一组，红字标题
│ ╰────────────────────────╯ │
│ ╭────────────────────────╮ │
│ │ 今天  2                 │ │
│ │ ○  法语课后作业          │ │
│ │ ◉  交团费               │ │
│ ╰────────────────────────╯ │
│ ╭────────────────────────╮ │
│ │ 以后  3                 │ │
│ │ ○  期中考试    ●考试 11.5│ │ ← 跨天的项显示日期
│ ╰────────────────────────╯ │
│ ╭────────────────────────╮ │
│ │ 已完成 4                │ │
│ ╰────────────────────────╯ │
│ ┌────────────────────────┐ │
│ │     + 添加日程          │ │ ← 主按钮，蓝色通栏
│ └────────────────────────┘ │
└────────────────────────────┘
```

**抽屉表单（日程新增/编辑）**

```
┌────────────────────────────┐
│ 添加日程                    │
│                            │
│ 标题                        │
│ ┌────────────────────────┐ │
│ │ 法语课后作业            │ │
│ └────────────────────────┘ │
│ 日期                        │
│ ┌────────────────────────┐ │
│ │ 2026-09-25        📅   │ │
│ └────────────────────────┘ │
│ 开始        结束             │
│ ┌───────┐  ┌───────┐       │
│ │ --:-- │  │ --:-- │       │ ← type=time，可留空 = 全天
│ └───────┘  └───────┘       │
│ 分类                        │
│ ┌────────────────────────┐ │
│ │ 作业               ▾   │ │
│ └────────────────────────┘ │
│ 备注                        │
│ ┌────────────────────────┐ │
│ │                        │ │
│ └────────────────────────┘ │
│                            │
│ [删除] [取消] [ 保存 ]      │ ← 编辑态才有删除
└────────────────────────────┘
```

关闭方式三种，全部支持：点遮罩、点取消、按 Esc。

---

## 7. 第一版必须完成的功能

按依赖顺序，F1–F3 是地基。

| 编号 | 功能 | 验收锚点 |
| --- | --- | --- |
| F1 | 校历换算：给定任意日期得出周次、给定周次得出 7 个日期 | 2026-09-25 → 第 3 周 |
| F2 | 节次换算：给定起止节次得出时刻 | 第 6–7 节 → 14:00–15:35 |
| F3 | 本地持久化：读写 `buaa-schedule:v1`，异常回落不白屏 | 刷新后数据仍在 |
| F4 | 课表按周次过滤 + 单双周能力（数据有 parity 字段，UI 暂不暴露） | 第 17 周网格为空 |
| F5 | `lessonsOn(date)` 合成：固定课表 → 减停课 → 加补课 → 排序 | 今日页与课表页结果一致 |
| F6 | 今日视图：日期头、课程时间线、已过置灰、今日待办、勾选 | 见第 3 章 |
| F7 | 课表视图（桌面网格 + 移动单日，两套都可用） | 390px 无横向滚动 |
| F8 | 周导航：翻周、钳制、回到本周、超期提示 | 无法翻到第 0 或第 20 周 |
| F9 | 日程增删改查 + 分类筛选 + 四组分组排序 | 逾期项出现在「已逾期」组 |
| F10 | 手动调课：停单日单节、补单日单节、撤销 | 撤销后课程恢复 |
| F11 | 节假日标注（仅显示，不影响排课） | 9/25 显示「中秋节」 |
| F12 | 响应式与底部导航 | 见 2.4 |
| F13 | 部署为可访问链接 | 手机浏览器打开可用 |

---

## 8. 第一版暂时不做的功能

| 类别 | 不做项 | 原因 |
| --- | --- | --- |
| 账号 | 注册、登录、多用户 | 仅个人使用，用户明确要求 |
| 社交 | 分享、协作、同学对比课表 | 用户明确要求 |
| 同步 | 多设备数据同步、云端后端、导出导入 | 用户选「先不管，单设备使用为主」 |
| 提醒 | 消息推送、日历订阅、浏览器通知 | 未列入需求 |
| 日程 | 重复型日程（每周例会）、地点字段、优先级、附件、子任务 | 用户未勾选重复日程 |
| 课表 | 在页面内编辑固定课表（改课程名/时段/周次范围） | 课表是编译期常量，页面内只允许停/补 |
| 课表 | 整周停课、临时换教室、课改到另一天 | 用户选的粒度是单日单节 |
| 课表 | 单双周课程的界面开关 | 本学期无此类课，字段留着，UI 不做 |
| 校历 | 节假日自动停课 | 见 R5，判错代价是漏课 |
| 校历 | 第二学期、第三学期、考试周安排 | 用户选「只做本学期」 |
| 界面 | 深色模式、主题自定义、字体大小设置 | 用户选浅色简洁 |
| 界面 | 倒计时、空闲教室、当前/下一节课高亮 | 用户在多选里未选 |
| 安全 | 访问口令、平台级访问限制 | 用户明确「仅靠链接私密，不加保护」 |

---

## 9. 可以实际检查的验收标准

每条都可人工执行并判断通过与否。AC-1 至 AC-9 必须在电脑 Chrome 与手机浏览器各跑一遍。

### 9.1 数据正确性

**AC-1 周次锚点**
打开 `#/today`，日期头显示「第 3 周」。与教务 App 顶部「2026-2027 秋季 第3周」一致。

**AC-2 今天真的有课**
2026-09-25 的今日课程时间线，第一项为「习近平新时代中国特色社会主义思想」，时间列显示 `08:00–11:25`，副行显示 `第1–4节`，教室「科研一号楼」。

**AC-3 节次时刻映射**
课表页任取一个跨第 6–7 节的色块（如周一综合法语(1)），其行位置对应 14:00 起、15:35 止；今日页该课程的 `sessionRange` 输出 `14:00–15:35`。

**AC-4 节假日标注**
今日页日期头出现红底「中秋节」标签。同一天课程**照常显示**（R5 要求）。

### 9.2 持久化

**AC-5 日程活过刷新**
新增一条日程「验收测试」，分类作业，日期今天。按 F5 刷新页面 → 该项仍在「今天」组且未勾选。关闭浏览器标签页后重新打开链接 → 仍在。

**AC-6 调课活过刷新**
把今天某一节停课，刷新 → 该节仍不在今日课程与课表网格中；「本周调课」卡片仍列出该记录。

**AC-7 存储损坏不白屏**
开发者工具执行 `localStorage.setItem('buaa-schedule:v1','{坏掉的JSON')` 后刷新 → 页面正常渲染，日程为空，控制台有 warn，无未捕获异常。

### 9.3 交互闭环

**AC-8 停课可撤销**
记录某节课（如周五第 6–7 节航空航天概论A）→ 课表页点该色块 → 抽屉点「停掉这一节」→ 该格消失，今日页（若为当天）同步消失 → 「本周调课」出现一条记录 → 点「撤销」→ 色块恢复，内容与停课前完全一致。

**AC-9 补课生效**
课表页点一个空格子 → 抽屉「补一节课」→ 填名称、起止节次 → 保存 → 该位置出现青绿色补课块，节次与所填一致 → 翻到下一周 → 该补课**不出现**（只影响那一天）→ 回到本周 → 仍在。

**AC-10 勾选联动**
在今日页把一条待办勾选完成 → 切到 `#/todos` → 该项位于「已完成」组，标题带删除线 → 在日程页取消勾选 → 回到今日页，它回到未完成区且顺序正确。

**AC-11 校验拦截**
新增日程时标题留空点保存 → 出现提示「标题和日期不能为空」，抽屉不关闭，已填内容不丢。补课时结束节次早于开始节次 → 提示「结束节次不能早于开始节次」。

**AC-12 周导航边界**
在第 1 周点 `‹` → 停在第 1 周，不报错。在第 19 周点 `›` → 停在第 19 周。翻到第 17 周 → 网格为空 + 提示「已超过第 16 周」。非当前周出现「回到本周」，点击后回到第 3 周。

### 9.4 响应式

**AC-13 手机无横向滚动**
390×844 下三个视图逐一检查：页面不出现横向滚动条；底部标签栏三等分且可点；课表显示为单日列表而非网格；抽屉从底部上滑，内容超长时可内部滚动，「保存」按钮可达。

**AC-14 桌面形态**
1280×800 下：导航在顶栏右侧；课表为 7×14 网格且整周不出现文字挤压重叠；抽屉居中显示，宽度不超过 440px。

**AC-15 旋转与拖窗**
手机横竖屏来回切换、桌面拖动浏览器窗口宽度跨过 768px → 网格与单日列表正确互换，无需刷新，无残留空白。

### 9.5 交付

**AC-16 本地直开**
双击 `E:\schedule\index.html`，在 `file://` 下三个视图全部可用（验证未误用 ES module 导致跨域失败）。

**AC-17 线上可用**
部署链接在手机浏览器打开后，AC-1 至 AC-13 全部重跑通过。此项**必须由用户本人在真机确认**，代理无法代测。

**AC-18 无控制台错误**
三个视图逐一操作一遍后，控制台无未捕获异常（warn 允许）。

---

## 附录 A · 已知限制（非缺陷，已获用户确认）

1. 日程与调课记录只存在单一浏览器，换设备后为空。用户回复「先不管，单设备使用为主」。
2. 链接公开可访问，课表含真实课程名与教室。用户回复「这是我个人使用的，没关系」。
3. 第 5.5 节 7 项推断未经逐条核对，如与实际课表不符，改 `js/courses.js` 对应字段即可，不涉及结构变更。

---

## 附录 B · 验收执行记录（2026-09-25）

代码骨架已存在于 `E:\schedule`。以下条目已在本地 `http://127.0.0.1:8765` 实测；未列出的条目**尚未验证**。

| 条目 | 结果 | 证据 |
| --- | --- | --- |
| AC-1 周次锚点 | 通过 | 2026-09-25 → 第 3 周；2026-09-07 → 1；2027-01-17 → 19；2027-01-18 → null；2026-09-06 → null |
| AC-2 今日课程 | 通过 | 今日页首项「习近平新时代中国特色社会主义思想 08:00–11:25 第1–4节 科研一号楼」 |
| AC-3 节次时刻 | 通过 | 第 6–7 节 → `14:00–15:35`；第 3–5 节 → `09:50–12:15` |
| AC-4 节假日标注 | 通过 | 日期头出现「中秋节」标签，且当天两节课照常显示 |
| AC-5 持久化 | 通过 | 写入 4 条日程后 `localStorage` 含 `{"version":1,"todos":[...]}` |
| AC-8 停课可撤销 | 通过 | 停掉航空航天概论A 后 `lessonsOn('2026-09-25')` 只剩 1 项；撤销后恢复为 2 项 |
| AC-9 补课生效 | 通过 | 在 2026-09-26 补第 6–7 节 → 该日出现「临时补课/14:00–15:35」，2026-09-27 仍为 0 项 |
| AC-7 存储损坏不白屏 | 部分通过 | 写入非法 JSON 后刷新，日程页正常渲染空态、无未捕获异常；`console.warn` 未被工具捕获，未确认 |
| AC-12 周次过滤 | 通过 | 第 17 周周一（2026-12-28）`lessonsOn` 返回 0 项 |
| AC-13 手机形态 | 通过 | 窄视口下为底部标签栏 + 单日列表，无横向滚动，星期 chip 带课数角标 |
| AC-14 桌面形态 | 通过 | 1200px 下 7×14 网格，色块跨行正确（周五 1–4、周二 3–5），今天列头高亮 |
| F1–F12 逻辑 | 通过 | 以脚本驱动 `lessonsOn` / `addTodo` / `addOverride` / `removeOverride` 全链路无报错 |

### 未验证 / 待办

| 条目 | 状态 |
| --- | --- |
| AC-6 调课记录活过刷新 | 未测（AC-5、AC-8 分别通过，风险低） |
| AC-10 勾选联动 | 未测（`toggleTodo` 逻辑已通过脚本验证，UI 联动未测） |
| AC-11 表单校验 | 未测。空标题保存确实触发了拦截提示，但抽屉在提示后意外关闭，需人工复现确认是否为真缺陷 |
| AC-15 旋转与拖窗 | 未测 |
| AC-16 `file://` 直开 | 未测 |
| AC-17 线上真机 | **未部署，必须由用户在真机确认** |
| AC-18 控制台无异常 | 部分通过，见 AC-7 |

### 测试过程中修掉的缺陷

`renderToday()` 内部直接赋值 `$view.innerHTML` 且无返回值，而 `render()` 用其返回值再次赋值，导致今日页整页被字符串 `"undefined"` 覆盖。已改为 `return head + lessonBlock + todoBlock`。

`.g-day` 依赖网格自动定位，与显式定位的 `.g-label` 混排存在错位风险，已改为显式 `grid-column`。
